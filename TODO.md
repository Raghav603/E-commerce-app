# TODO - Connect FilterModal to HomeScreen

- [x] Update `navigation/HomeScreen.js` to open `FilterModal` from the existing Filters button.
- [x] Add modal state (visible/close handlers) and pass theme.
- [ ] Wire `onApply` from the modal to `useHomeViewModel` (price/rating/discount) so Home list refreshes.
- [x] Ensure modal closes after applying.
- [ ] Run app/tests (if available) to verify UI flow (jest preset currently missing).


